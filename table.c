#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    int i,result;
    int n=2;
        for (i=1;i<=10;i++){
            result=2*i;
            printf("%d x %d = %d\n ",n,i,result);
        }
    return 0;
}
